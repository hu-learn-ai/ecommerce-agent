# ============================================
# Docker 构建上下文排除（加速构建）
# data/ 和 models/ 由 docker-compose 以 volume 挂载，
# 无需打进镜像（否则构建上下文 460MB+，传输极慢）
# ============================================

.git
__pycache__
*.pyc
.idea
.pytest_cache
.workbuddy
.private_backup

.env
.env.docker
.env.example
venv

# 大数据目录（volume 挂载，不进入镜像）
data
models
淘宝用户消费数据汇总

docs
tests

# 原始数据集与本地独占资产（与 .gitignore 对齐）：
# 缺了这些，本地构建上下文会被 1.7GB 的原始 JData 数据撑爆
新建文件夹
finetune
outputs
评估结果2
面试深挖题库_电商Agent项目.html
mysql_gmall.sql

*.tar.gz
*.log
structure.txt
